import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mclinic-nurse-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
